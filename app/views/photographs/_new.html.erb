<%= simple_form_for [photographer, photograph] do |f|%>
  <%= f.input :photo, as: :file, input_html: { multiple: true }, label:"Add Photos" %>
  <%= f.submit "Upload", class: "btn btn-dark" %>
<% end %>
