this is where I'm working

<div class="row justify-content-center">
  <% @photographers.each do |photographer| %>
    <!-- <div class="card card-photographer m-3" style="width: 18rem;">
      <div style="background-image: linear-gradient(rgba(0,0,0,0.3), rgba(0,0,0,0.3)), url('<%= cl_image_path photographer.photo.key, cloud_name: 'pedrosafontes', height: 300, width: 400, crop: :fill %>')" class="card-photographer-img">
      </div>
      <div class="card-body text-center">
        <h5 class="card-title"><%= link_to photographer.name, photographer_path(photographer.id) %></h5> -->
     photographer.name
      </div>
    </div>
  <% end %>
</div>
