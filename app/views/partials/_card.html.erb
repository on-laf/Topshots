<div class="card">
  <%= cl_image_tag photographer.photographs[0].photo.key %>
  <div class="card-infos">
    <div>
      <h2><%= photographer.name %></h2>
      <p><%= photographer.location %></p>
    </div>
    <h2 class="card-pricing"><%= photographer.price %> €</h2>
    <img src="https://kitt.lewagon.com/placeholder/users/random" class="card-avatar avatar-bordered" />
  </div>
</div>

